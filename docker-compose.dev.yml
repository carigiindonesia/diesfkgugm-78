services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: diesfkgugm-78:local
    env_file: .env.docker

  mysql:
    env_file: .env.docker
    ports:
      - "3306:3306"

  worker:
    image: diesfkgugm-78:local
    env_file: .env.docker
